/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SlicePipe } from './slice.pipe';

describe('Pipe: Slice', () => {
  it('create an instance', () => {
    let pipe = new SlicePipe();
    expect(pipe).toBeTruthy();
  });
});
